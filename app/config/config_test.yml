imports:
    - { resource: config_dev.yml }
parameters:
    domain: 'infopaginas-qa.oxagile.com'
framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true
