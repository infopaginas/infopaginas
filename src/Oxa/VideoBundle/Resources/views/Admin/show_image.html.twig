{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    {% if object.id is not null and object.poster %}
        {% thumbnail attribute(object, 'poster' ), 'normal' with {'width': 500, 'class': 'video-poster'} %}
    {% endif %}
{% endblock %}
