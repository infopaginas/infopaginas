{% for subcategory in subcategories %}
    {% if currentSubcategory %}
        {% set is_selected = (subcategory.id == currentSubcategory.id) %}
    {% else %}
        {% set is_selected = false %}
    {% endif %}

    <a href="{{ absolute_url(path('domain_search_catalog', {'localitySlug': currentLocality.getSlug(), 'categorySlug': currentCategory.getSlug(), 'categorySlug2': subcategory.getSlug() })) }}">
        {{ subcategory.getName() }}
    </a>
    |
{% endfor %}